apiVersion: v1
kind: Service
metadata:
  name: integrations-app
  namespace: integrations
spec:
  selector:
    project: integrations
    service: integrations
    type: app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
